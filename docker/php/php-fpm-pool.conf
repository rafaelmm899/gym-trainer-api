[www]
user = app
group = app

listen = 9000
listen.owner = app
listen.group = app

; Pass container environment variables through to PHP.
clear_env = no

pm = dynamic
pm.max_children = 20
pm.start_servers = 3
pm.min_spare_servers = 2
pm.max_spare_servers = 6
